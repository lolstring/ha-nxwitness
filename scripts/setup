#!/usr/bin/env bash
# Install development dependencies ( integration_blueprint style).
set -euo pipefail
cd "$(dirname "$0")/.."

if command -v uv >/dev/null 2>&1; then
    uv pip install --system -r requirements_dev.txt
else
    python3 -m pip install -r requirements_dev.txt
fi
